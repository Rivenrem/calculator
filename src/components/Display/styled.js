import styled from "styled-components";

export const Input = styled.input`
  background-color: var(--gray);
  margin: 1rem 0;
  padding: 2rem 1rem;
  width: 20rem;
  border-radius: 4rem;
  text-align: center;
  font-size: x-large;
`;
