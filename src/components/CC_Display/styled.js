import styled from "styled-components";

export const Input = styled.input`
  width: 100%;
  margin: 1rem 0;
  padding: 2rem 1rem;
  font-size: x-large;
  text-align: center;
  overflow: scroll;
  border-radius: 4rem;
  background-color: var(--gray);
`;
